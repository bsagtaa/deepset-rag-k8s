apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-query
  labels:
    {{- include "haystack-rag.labels" . | nindent 4 }}
    app: {{ .Release.Name }}-query
spec:
  replicas: {{ .Values.replicaCount.query }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-query
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-query
        {{- include "haystack-rag.labels" . | nindent 8 }}
    spec:
      containers:
        - name: query
          image: "{{ .Values.image.query.repository }}:{{ .Values.image.query.tag }}"
          imagePullPolicy: {{ .Values.image.query.pullPolicy }}
          ports:
            - containerPort: 8002
              name: http
          env:
            {{- range $key, $value := .Values.environment.query }}
            - name: {{ $key }}
              value: {{ $value | quote }}
              {{- end }}
            - name: OPENSEARCH_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: opensearch-user
            - name: OPENSEARCH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: opensearch-password
          volumeMounts:
            - name: file-storage
              mountPath: /app/files
            - name: pipelines
              mountPath: /app/pipelines
          resources:
            {{- toYaml .Values.resources.query | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: file-storage
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-file-storage
        - name: pipelines
          emptyDir: {}
